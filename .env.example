# TMDB_PROXY_BASE_URL: base URL of the CineLog TMDB proxy (see /proxy).
# Lets the app work out of the box without asking users for their own
# TMDB API key. Leave empty to fall back to "personal key required".
TMDB_PROXY_BASE_URL="https://cinelog-tmdb-proxy.general-grievous0401.workers.dev"
